import { Spinner } from '../constants';

const AboutUs = () => {
  return (
    <main className='text-black w-full h-full'>
      <div>
        <Spinner />
      </div>
      AboutUs
    </main>
  );
};

export default AboutUs;
